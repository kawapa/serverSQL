#include "Parser.hpp"

#include <cstring>
#include <sstream>

using namespace std::string_literals;

// void Parser::generateAnswer(char* message, bool status) {
//     if (status) {
//         message = R"({ "status": "ok" })";
//     }
//     else {
//         message = R"({ "status": "error" })";
//     }
// }

// void Parser::generateAnswer(char* message, std::string message1, int64_t seconds, std::string message2) {
//     // std::stringstream ss;
//     // ss << "Server has started " << seconds << " seconds ago";
//     // strcpy(message, "Server has started ");
// }